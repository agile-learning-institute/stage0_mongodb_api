configurations:
  name: Test Configurations Endpoints
  steps:
    - name: Placeholder Step
      http:
        url: http://${{env.host}}/api/configurations
        method: GET
        check:
          status: /200/
