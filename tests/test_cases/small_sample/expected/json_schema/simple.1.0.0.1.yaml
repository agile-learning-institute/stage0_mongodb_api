type: object
properties:
  _id:
    type: string
    pattern: "^[0-9a-fA-F]{24}$"
  name:
    type: string
    pattern: "^[a-zA-Z0-9\\s.,!?-]{4,255}$"
  status:
    type: string
    enum: ["draft", "active", "archived"]
